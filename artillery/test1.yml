config:
  target: "https://baza-trainee.github.io/1001-songs-frontend/#/"
  phases:
    - duration: 10
      arrivalRate: 2000
      name: Artillery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "{{ target }}get"
